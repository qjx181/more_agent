# 项目三 — 自主优化引擎 前置要求

## 项目三当前状态（2026-05-18 22:18 暂停）

cronjob 已暂停。下次启动 `cronjob action='resume' job_id='79cb9d06dc5d'` 即可。

## 项目三已经积累的能力

### 1. 经验库（self_evolve_log.json）
- coordinator write_file 比 delegate 更可靠（接口不变改造、新测试文件）
- qwen2.5:7b 子Agent会改坏函数签名，复杂任务不走它
- diff_content_check 发现了假产出
- DeepSeek 做协调者工具调用能力强于 8B 模型
- StreamingResponse generator 内部不能 raise HTTPException

### 2. 已有 Skill
- selfevolve-check-async — 验证 async def 改造是否到位

### 3. 企业级质量标准（内置到 prompt）
核心模块 90%+ 测试覆盖、I/O 必须 async def、外部调用 try/except、硬编码抽 config、死代码清理、限流/鉴权/输入校验完整

### 4. 省 token 策略
技能按需加载、自己写的代码不送审、反思简版格式

## 接入新项目需要准备的

### 前置步骤（我来做）
1. 停止当前 cronjob
2. 修改 cronjob 的 workdir 指向新项目目录
3. 更新 TODO.md 为新项目的目标
4. 可选：更新 self_evolve_log.json 的 `accumulated_insights`（保留通用经验，清理项目一特定内容）

### 新项目需要满足的条件
1. 项目代码在 WSL 可访问的路径下（/mnt/... 或 /home/...）
2. 项目的 pip 依赖已安装（或项目三可以 pip install）
3. 项目是一个 Python 项目（支持 ast.parse 语法检查）
4. 最好已有基础的测试框架（pytest）

## 启动方式
```
cronjob action='resume' job_id='79cb9d06dc5d'
```
或者修改 prompt 后重新 focus 到新项目。
