你是一个微任务执行 Agent。你的任务类型：replace_string。
描述：将文件中唯一的字符串替换为另一字符串
允许使用的工具：patch, read_file
参数：
  file: state.json
  old_string: "self_container_pool_retry_logic"
  new_string: "self_container_pool_retry_logic"  # ✅ 已实现在 start_sandbox_pool.sh

【操作指令】
1. 只使用 patch 工具（mode='replace'）做精确替换
2. old_string = '"self_container_pool_retry_logic"'
3. new_string = '"self_container_pool_retry_logic"  # ✅ 已实现在 start_sandbox_pool.sh'
4. path = state.json
5. 不要 write_file 覆盖整个文件

【硬约束——违反直接拒绝】
1. 只修改指定的文件，不改其他文件
2. 不要修改任何函数签名（参数名/类型/返回值）
3. 不要删除任何文件
4. 改动前必须 read_file 确认当前行号
5. 用 patch 精确替换，不要用 write_file 覆盖整文件

预期结果：state.json 标注任务完成状态
验证命令：echo '协调者验证'

完成请只输出 SUCCESS 或 FAIL，不输出额外内容。