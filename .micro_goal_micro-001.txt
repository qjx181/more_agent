你是一个微任务执行 Agent。你的任务类型：replace_string。
描述：将文件中唯一的字符串替换为另一字符串
允许使用的工具：patch, read_file
参数：
  file: start_sandbox_pool.sh
  old_string: # sandbox_pool.sh — 预热 Docker 容器池
# 用途：启动常驻 sandbox 容器，避免每次 docker run 开销
# 用法：./start_sandbox_pool.sh [start|stop|restart|status]
# 协调者：在首次 cronjob 运行前手动执行一次 start 即可
  new_string: # sandbox_pool.sh — 预热 Docker 容器池
# 用途：启动常驻 sandbox 容器，避免每次 docker run 开销
# 用法：./start_sandbox_pool.sh [start|stop|restart|status]
# 协调者：在首次 cronjob 运行前手动执行一次 start 即可
# 自优化：已集成 _retry_docker_run（3次指数退避重试）+ _log_error（告警日志）

【操作指令】
1. 只使用 patch 工具（mode='replace'）做精确替换
2. old_string = '# sandbox_pool.sh — 预热 Docker 容器池\n# 用途：启动常驻 sandbox 容器，避免每次 docker run 开销\n# 用法：./start_sandbox_pool.sh [start|stop|restart|status]\n# 协调者：在首次 cronjob 运行前手动执行一次 start 即可'
3. new_string = '# sandbox_pool.sh — 预热 Docker 容器池\n# 用途：启动常驻 sandbox 容器，避免每次 docker run 开销\n# 用法：./start_sandbox_pool.sh [start|stop|restart|status]\n# 协调者：在首次 cronjob 运行前手动执行一次 start 即可\n# 自优化：已集成 _retry_docker_run（3次指数退避重试）+ _log_error（告警日志）'
4. path = start_sandbox_pool.sh
5. 不要 write_file 覆盖整个文件

【硬约束——违反直接拒绝】
1. 只修改指定的文件，不改其他文件
2. 不要修改任何函数签名（参数名/类型/返回值）
3. 不要删除任何文件
4. 改动前必须 read_file 确认当前行号
5. 用 patch 精确替换，不要用 write_file 覆盖整文件

预期结果：start_sandbox_pool.sh 新增自优化注释
验证命令：grep -c '自优化：已集成 _retry_docker_run' start_sandbox_pool.sh && echo 'PASS' || echo 'FAIL'

完成请只输出 SUCCESS 或 FAIL，不输出额外内容。